import LoadingModal from '../components/LoadingModal';

const Loading = () => {
  // Next js has a built-in loading component that can be used to show a loading spinner while the page is loading.
  return <LoadingModal />;
};

export default Loading;
